{
  "//": "PreToolUse require-skill gate — repo-owned. An edit whose new text has at least minHangul Hangul characters is denied until the skill named by the first matching path rule has been invoked in this session (marker + ttlHours; a context compaction drops the skill, so the window is short). Deny goes to the model, not the user.",
  "minHangul": 20,
  "ttlHours": 2,
  "exclude": ["node_modules/**", ".claude/**", "**/*.lock", "**/*.jsonl"],
  "rules": [
    { "skill": "ko-ui-text", "paths": ["**/*.jsx", "**/*.tsx", "**/*.vue", "**/*.html", "**/i18n/**", "**/locales/**"] },
    { "skill": "ko-writing", "paths": ["**"] }
  ]
}
